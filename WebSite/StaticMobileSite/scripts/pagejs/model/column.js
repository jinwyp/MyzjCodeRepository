define(function (require, exports, module) {

    var Backbone = require("backbone");

    var Model = Backbone.Model.extend({
        defaults: {
    },
    initialize: function () {
    },
    validation: {
}
});

return Model;

});